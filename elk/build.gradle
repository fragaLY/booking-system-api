plugins {
    id 'com.palantir.docker'
}

group 'by.vk'

def logStash = "logstash-tcp-input.conf"

docker {
    name "fragaly/elk"
    dockerfile file('Dockerfile')
    files projectDir.absolutePath
    buildArgs(['LOGSTASH_INPUT_CONFIG': logStash])
    pull false
    noCache true
}