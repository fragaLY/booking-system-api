apiVersion: backend
kind: deployment
metadata:
  name: backend
spec:
  replicas: 3
  selector:
    matchlabels:
      tier: backend
template:
  metadata:
    labels:
      tier: backend
  spec:
    containers:
      -name: fragaly/booking-system
      image: fragaly/booking-system